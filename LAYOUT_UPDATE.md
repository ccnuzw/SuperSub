# 布局系统更新说明

本项目已成功实现了双布局系统，支持左右布局和上下布局的动态切换。

## 新增文件

1. **src/stores/layout.ts** - 布局状态管理
2. **src/components/VerticalLayout.vue** - 左右布局组件
3. **src/components/HorizontalLayout.vue** - 上下布局组件
4. **src/components/AdaptiveLayout.vue** - 自适应布局主组件

## 功能特性

### 1. 左右布局 (Vertical Layout)
- 顶部：Logo + 主题切换 + 布局切换按钮
- 左侧：可折叠侧边栏导航菜单
- 右侧：主内容区域
- 移动端：抽屉式菜单

### 2. 上下布局 (Horizontal Layout)
- 顶部第一行：Logo + 用户操作区（主题切换、布局切换、用户菜单）
- 顶部第二行：水平导航菜单
- 下方：主内容区域
- 移动端：可滚动的水平按钮菜单

### 3. 布局切换方式
- **快速切换**：点击顶部导航栏中的布局切换图标
- **设置页面**：在设置 > 通用设置中选择布局模式

### 4. 状态持久化
- 布局模式自动保存到 localStorage
- 页面刷新后保持用户选择的布局模式

### 5. 响应式设计
- 两种布局都支持移动端适配
- 根据屏幕尺寸自动调整布局细节

## 使用说明

1. **切换布局**：
   - 方法一：点击顶部导航栏的布局切换图标（Resize图标）
   - 方法二：进入设置页面，在"界面布局设置"中选择布局模式

2. **布局特点**：
   - **左右布局**：适合传统桌面应用体验，导航固定在侧边
   - **上下布局**：适合内容展示，顶部导航节省垂直空间

## 技术实现

- 使用 Pinia store 管理布局状态
- 组件化设计，便于维护和扩展
- 响应式布局，支持各种屏幕尺寸
- 平滑的切换动画效果